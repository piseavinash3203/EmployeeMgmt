package io.springboot.employee;

import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.GetMapping;

@Service
public class EmployeeService1 {
	
	/*
	 * @SuppressWarnings("unchecked") private Map<Integer, Employee1> employees =
	 * createList();
	 * 
	 * @GetMapping(produces = "application/json") public Map<Integer, Employee1>
	 * firstPage() { return employees; }
	 * 
	 * private Map<Integer, Employee1> createList() { Map<Integer,Employee1>
	 * tempEmployees = new HashMap<Integer,Employee1>(); Employee1 emp1 = new
	 * Employee1(); emp1.setName("emp1"); emp1.setDesignation("manager");
	 * emp1.setEmpId("1"); emp1.setSalary(3000);
	 * 
	 * Employee1 emp2 = new Employee1(); emp2.setName("emp2");
	 * emp2.setDesignation("developer"); emp2.setEmpId("2"); emp2.setSalary(3000);
	 * 
	 * tempEmployees.put(1,emp1); tempEmployees.put(2,emp2); return tempEmployees; }
	 * 
	 * public Employee1 delete(int id) { //Employee emp : employees Employee1
	 * deletedEmp = null; for (Map.Entry<Integer,Employee1> emp :
	 * employees.entrySet()) { if (((Employee1) emp).getEmpId().equals(id)) {
	 * employees.remove(emp); deletedEmp = (Employee1) emp; break; } } return
	 * deletedEmp; }
	 * 
	 * public Employee1 add(Employee1 user) { ((EmployeeService1)
	 * employees).add(user); return user; }
	 */
	
	
}
